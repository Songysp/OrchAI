services:
  orchestrator:
    build:
      context: ..
      dockerfile: infra/Dockerfile
    container_name: ai-dev-platform-orchestrator
    ports:
      - "8000:8000"
    volumes:
      - ../configs:/app/configs
      - ../workspaces:/app/workspaces
      - ../data:/app/data
